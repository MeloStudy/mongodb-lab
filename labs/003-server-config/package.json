{
  "name": "lab-003-server-config",
  "version": "1.0.0",
  "description": "Base infrastructure validation for MongoDB Labs",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  }
}
