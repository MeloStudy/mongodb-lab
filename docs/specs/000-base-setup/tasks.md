# LAB-000 Tasks

- `[x]` Define directory structure at root or in a dedicated `/labs/000-base-setup` folder.
- `[x]` Create `docker-compose.yml` for MongoDB 7.0 standard startup.
- `[x]` Initialize base `package.json` with Jest and MongoDB Node Driver.
- `[x]` Write standard `Makefile` with `setup`, `test`, `clean` targets.
- `[x]` Write a dummy `validator.test.js` to ensure the Node API can connect to Docker DB.
- `[x]` Test `make clean` to verify complete Atomic Cleanup capabilities.
- `[x]` Document usage in `README.md`.
